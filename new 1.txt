execution_task
--------------
id (PK)

task_type_id (FK)
task_category_id (FK)
status_id (FK)

planned_date
planned_start_time
planned_end_time

start_time
end_time

remarks


production_task
---------------
id (PK, FK → execution_task.id)

ordered_product_id (FK)
production_batch_id (FK, NOT NULL)
recipe_id (FK, NOT NULL)

resource_id (FK, NOT NULL)   -- line, mixer, etc.

target_storage_location_id (FK)       -- optional
``


bigbag_filling_task
-------------------
id (PK, FK → execution_task.id)

production_batch_id (FK)        -- source of material
resource_id (FK, NOT NULL)      -- filling line

target_storage_location_id (FK, NOT NULL)


maintenance_task
----------------
id (PK, FK → execution_task.id)

maintenance_plan_id (FK)
maintenance_type_id (FK, NOT NULL)

resource_id (FK)
resource_component_id (FK)

scheduled_by_plan (BOOLEAN)